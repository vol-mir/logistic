<div class="card-body">

    {{ form_row(form._token) }}

    <div class="form-group">
        {{ form_label(form.point_name) }}
        {{ form_widget(form.point_name) }}

        {% if form.point_name.vars['errors']|length > 0 %}
            <ul class="list-unstyled">
                {% for error in form.point_name.vars['errors'] %}
                    <li class="text-danger small">{{ error.message }}</li>
                {% endfor %}
            </ul>
        {% endif %}
    </div>
    <div class="form-group">
        {{ form_label(form.postcode) }}
        {{ form_widget(form.postcode) }}

        {% if form.postcode.vars['errors']|length > 0 %}
            <ul class="list-unstyled">
                {% for error in form.postcode.vars['errors'] %}
                    <li class="text-danger small">{{ error.message }}</li>
                {% endfor %}
            </ul>
        {% endif %}
    </div>
    <div class="form-group">
        {{ form_label(form.country) }}
        {{ form_widget(form.country) }}

        {% if form.country.vars['errors']|length > 0 %}
            <ul class="list-unstyled">
                {% for error in form.country.vars['errors'] %}
                    <li class="text-danger small">{{ error.message }}</li>
                {% endfor %}
            </ul>
        {% endif %}
    </div>
    <div class="form-group">
        {{ form_label(form.region) }}
        {{ form_widget(form.region) }}

        {% if form.region.vars['errors']|length > 0 %}
            <ul class="list-unstyled">
                {% for error in form.region.vars['errors'] %}
                    <li class="text-danger small">{{ error.message }}</li>
                {% endfor %}
            </ul>
        {% endif %}
    </div>

    <div class="form-group">
        {{ form_label(form.city) }}
        {{ form_widget(form.city) }}

        {% if form.city.vars['errors']|length > 0 %}
            <ul class="list-unstyled">
                {% for error in form.city.vars['errors'] %}
                    <li class="text-danger small">{{ error.message }}</li>
                {% endfor %}
            </ul>
        {% endif %}
    </div>

    <div class="form-group">
        {{ form_label(form.locality) }}
        {{ form_widget(form.locality) }}

        {% if form.locality.vars['errors']|length > 0 %}
            <ul class="list-unstyled">
                {% for error in form.locality.vars['errors'] %}
                    <li class="text-danger small">{{ error.message }}</li>
                {% endfor %}
            </ul>
        {% endif %}
    </div>

    <div class="form-group">
        {{ form_label(form.street) }}
        {{ form_widget(form.street) }}

        {% if form.street.vars['errors']|length > 0 %}
            <ul class="list-unstyled">
                {% for error in form.street.vars['errors'] %}
                    <li class="text-danger small">{{ error.message }}</li>
                {% endfor %}
            </ul>
        {% endif %}
    </div>
</div>
<!-- /.card-body -->

