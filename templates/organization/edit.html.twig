{% extends 'base.html.twig' %}

{% block title %}Редактирование организации{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-12">
            <!-- jquery validation -->
            <div class="card card-default">
                <!-- form start -->
                <form id="organizationForm" action="{{ path('organization_edit', {'id': organization.id}) }}" method="POST">
                    {% include 'organization/_form.html.twig' with {'form': form} only %}
                    <div class="card-footer">
                        <button type="submit" class="btn btn-primary">{{ 'title.save'|trans }}</button>
                        <a href="{{ path('organization_index') }}" class="btn btn-default float-right">{{ 'title.cancel'|trans }}</a>
                    </div>
                </form>
            </div>
            <!-- /.card -->
        </div>
    </div>
    <!-- /.row -->

{% endblock %}


{% block javascripts %}
    <!-- jquery-validation -->
    <script src="{{ asset('plugins/jquery-validation/jquery.validate.min.js') }}"></script>
    <script src="{{ asset('plugins/jquery-validation/additional-methods.min.js') }}"></script>

    <script type="text/javascript">
        $(document).ready(function () {

            $('#organizationForm').validate({
                ignore: ".ignore-validate",
                rules: {
                    'organization[registration_number]': {
                        required: true,
                        maxlength: 190
                    },
                    'organization[abbreviated_name]': {
                        required: true,
                        maxlength: 190
                    },
                    'organization[full_name]': {
                        required: true,
                        maxlength: 190
                    }
                },
                messages: {
                    'organization[registration_number]': {
                        required: "{{ 'message.validation.required'|trans }}",
                        maxlength: "{{ 'message.validation.too_long_content'|trans({ '%limit%': 190 }) }}"
                    },
                    'organization[abbreviated_name]': {
                        required: "{{ 'message.validation.required'|trans }}",
                        maxlength: "{{ 'message.validation.too_long_content'|trans({ '%limit%': 190 }) }}"
                    },
                    'organization[full_name]': {
                        required: "{{ 'message.validation.required'|trans }}",
                        maxlength: "{{ 'message.validation.too_long_content'|trans({ '%limit%': 190 }) }}"
                    },
                },
                errorElement: 'div',
                errorPlacement: function (error, element) {
                    error.addClass('invalid-feedback');
                    element.closest('.form-group').append(error);
                },
                highlight: function (element, errorClass, validClass) {
                    $(element).addClass('is-invalid');
                },
                unhighlight: function (element, errorClass, validClass) {
                    $(element).removeClass('is-invalid');
                },
            });
        });
    </script>

{% endblock %}